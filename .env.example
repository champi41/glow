# URL pública de la app (opcional).
# Si no se define, se usa la URL actual (funciona en producción y al abrir el link en el mismo navegador).
# Útil en desarrollo: pon aquí la URL de un túnel (ngrok, etc.) para que el link de reseña por WhatsApp funcione desde el móvil.
# VITE_PUBLIC_APP_URL=https://tu-app.ngrok.io
